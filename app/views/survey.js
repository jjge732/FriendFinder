// let newSurvey = {
//     name: $('#name').val(),
//     photo: $('#photo').val(),
//     scores: []
// };

// for (let i = 1; i <= questionNumber; i++) {
//     newSurvey.scores.push(parseFloat($(`.surveyQuestion #question${i}`).val()));
// };

const popUp = event => {
    event.preventDefault();
    // let modal = document.getElementById('modal');
    // // https://davidwalsh.name/fakepath
    // let source = document.getElementById('photo').value.replace('C:\\fakepath\\', '');
    // console.log(source);
    // modal.innerHTML = `<img id='photo' src='images/${source}' alt='matchImage'>`
}